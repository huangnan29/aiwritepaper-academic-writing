# 适配：Codex

记录`AGENT_ADAPTER=codex`，只按当前工具事实行动。当前会话有imagegen/image_gen时，所有适合生图的结构图逐张调用并保存原图、Prompt与回执；父任务可代调时记PARENT_AGENT。成功图或其中文覆盖PNG必须成为final_embed_file，不能改插SVG。无真实图片工具才SVG降级。DOCX、PDF和页面检查均使用实际可用能力；缺口写CAPABILITY_GAP。
