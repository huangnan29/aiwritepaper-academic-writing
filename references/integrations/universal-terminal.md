# 适配：通用终端Agent

记录`AGENT_ADAPTER=universal-terminal`。适用于OpenCode、Copilot、ZCode、DeepSeek-tui等；按当前、父层、MCP/插件和客户端真实工具填写能力，不按模型品牌推断。任一层可生图时逐张调用并保存回执，不能因子任务无工具改走SVG。无真实工具才降级。DOCX/PDF、视觉与最终状态必须来自实际文件和四类检查，缺口如实报告。
