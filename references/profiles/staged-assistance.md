# 分阶段执行辅助

仅GUIDED和WEAK_MODEL使用。根据内嵌模板建立检查点，依次完成证据、大纲、正文、图片、文档和核验，不向用户逐阶段确认。每次只处理当前阶段并保存真实输出；接近上下文上限时写入进度、文件、问题和下一动作。错误只返回受影响阶段，禁止全面重写；已核验文献、成功生图和OMML公式不得被低质量替代。最终状态只读14-adjudicated-status.json。

辅助针对实际错误：只给当前局部一个正确示例、一个反例和修复目标，不套全篇模板，不额外跑能力探针。没有错误的部分保留模型自主判断；工具或材料缺失如实报告，不能放宽真实性底线。
