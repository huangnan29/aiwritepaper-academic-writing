# 单提示词剪枝实施计划

## 范围

本地候选改造，不提交、不打标签、不分发到已安装Agent，不修改历史论文。v2.0.0标签作为原始对照。候选版本不得声称已验证90分。

## 设计

1. 公共规则保持单一事实来源，缩短重复条款；完整/紧凑提示词由同一组模块生成。
2. 模型先选择任务模式、方向和需要的图表/公式模块，合成器只确定性拼接；执行时仍只读一份MD。
3. 保留现有合成CLI及输出文件兼容；新任务清单按模式过滤模块，禁止续跑重建提示词。
4. 保留文献、数据、图片与文件真实性约束。排版、哈希和派生审计视图交给工具；工具不得自动作出语义PASS或填分数。
5. Profile只依据明确执行失败提供辅助，不能把缺材料、缺工具、诚实设计稿误判成弱模型。
6. 修复PDF空目录、Word重复题注及多行表格字数偏差。结论比例与词频仅作审稿提示。
7. 方向方法门按实际方法适用，保留专业错误检查，不把DID要求泛化到所有政策研究。

## 验证

- 先运行现有Python与Node测试作为基线。
- 新增模式隔离、同源编译、错误选择拦截、旧CLI兼容、审计视图不制造证据、Profile原因分类及真实排版缺陷fixtures。
- 全量编译、同步验证、测试、差异检查及提示词体积统计。
- 真实论文A/B测试另行执行；本轮本地静态及合成验证不能替代跨模型质量实验。

## 本地验证结果

- 原基线：121项Python测试通过；改造后165项Python与10项Node测试通过。
- 19方向full/compact同源、模式组合、路由及必要Schema检查通过；Skill入口校验与git diff --check通过。
- 以电路方向为例，完整兼容目录95,261字节降到约44KB；典型全文规则约37KB，全选约42KB；单独配图约16KB、带公式导出约17KB。以上不含用户参数和Agent适配器。
- 入口SKILL.md从13,914字节减至8,494字节；README从33,549字节减至约8.6KB（候选限制说明后略有增加）。
- 六份历史原稿只读回归：Sol真实目录通过；Grok五份空目录/无页码目录均被拒绝；电路7图与《边城》4图的重复题注检出，其余不误报。读取前后正文、DOCX和PDF摘要一致。
- 新Markdown计数剔除了Sol的Pandoc多行表格，约25,158有效单位；这与此前DOCX普通文本字符统计不是同一口径，不据此改写旧评分报告。

## 未完成与发布限制

### 2026-08-30：流程层精简结果

- 模型主流程收敛为准备、完整生产、统一检查、定点修复。paper.py prepare只接受模型已作出的方向/方法/能力判断，生成兼容准备文件，不生成正文或结论。
- paper.py check按模式调用实际检查器，保存当次日志和旧报告，汇总到12-final-qa-report.md；失败不沿用旧PASS，0退出码可以正确保留PARTIAL。
- 新Manifest使用DERIVED_ONLY，取消三层作者自报状态；DOCX/PDF路径与摘要由检查入口登记，研究性质仍需模型明确，不能自动猜测。
- 原地生产与只读审计分开：AUDIT_ONLY复制到源目录外的新目录，历史Profile版本差异只提示；绝对能力路径映射到已绑定摘要的副本文件，不访问原目录之外材料。
- 本地198项Python测试和10项Node测试通过，19方向同步与入口检查通过；入口SKILL.md为5,610字节、61行（首轮为8,494字节）。CI已补uv/PDF测试依赖，但尚未触发远端运行。
- 用历史Sol APOS稿完成真实副本审计，输出PARTIAL并保留历史Profile提示；原始Manifest、正文、DOCX和PDF未改变。这是入口与文件保护验证，不是重新生产论文或新的质量评分。

### 仍未验证

full/compact现在共用完整模块目录，compact兼容文件的体积从旧版约15KB增加至约44KB。实际入口通过task-selection提取需要的规则；这不自动证明弱模型输入更轻或完成率更高。发布前必须检查弱模型输入预算和真实产物，必要时进一步设计同源核心/操作说明分层，不能恢复漂移的两套标准。

未运行真实论文A/B、未扩大跨方向评估、未提交发布或分发。当前标记2.1.0-dev只标识本地开发候选，不是性能达标版本。
