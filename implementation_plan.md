# 2.1.0候选升级实施记录

## 目标

依据Fable 5.1审查，恢复“模型一次决策、单一MD执行”的主流程；把作者自评分移出写作运行时，提高19方向专业内容占比，恢复真正短小的弱模型路径，并以Grok标杆和Gemini弱模型观察作为真实论文发布证据。

## 已完成

### 运行时边界

- `paper.py prepare/check`保留为两个确定性入口。
- 写作提示词、检查矩阵和裁决器移除数字评分、SELF/ISOLATED身份和QUALITY第五报告。
- `qa-observations.json`只绑定具体主张、逐图、页面和问题记录。
- `eval/build_review_package.py`在写作结束后冻结独立评审输入，评分不影响权威状态。

### 准备与修订

- 四项能力改为true/false/null，未知不再等同不可用。
- 19方向提供DEFAULT_FEATURES，模型只声明feature_overrides。
- TITLE_POLICY支持STRICT、ASK、SAFE_DOWNGRADE三种边界。
- `paper.py prepare --amend`保留旧提示词，生成版本化新提示词与摘要，禁止移除已有产物模块，并记录失效阶段。
- RESUME读取Manifest中的活动提示词与活动合成报告。

### 专业方向

- 19个方向均补充问题类型、方法成立条件、专业对象闭合、证据边界、常见错误、图表语义和结论限制。
- 构建校验要求当前方向正文加方法门占最终方向提示词至少30%。
- 电子电路新增连接表，数学教育新增概念边表，综述新增分类抽查表；检查器只核结构与绑定，不冒充专业正确。

### Compact与适配

- 公共与方向源使用CORE标记；full读取完整内容，compact只取同源CORE。
- 合并GUIDED与WEAK_MODEL阶段辅助。
- 六个Agent适配器仅保留真实工具差异。
- 按最长适配器、阶段卡与1500字节参数预算，19方向WEAK_MODEL最终输入均不超过15KB。

### 机械验收

- 权威运行时只有证据、图片、公式和交付四类检查。
- 保留文献全文定位、数据来源、ImageGen实际嵌入、中文图、OMML、目录、题注、表格缩进、正文长度和文件摘要门。
- 成功生图不能被SVG替换；精确领域图必须读取适用源表。

### 精简真实论文评测

- 取消18次版本A/B和30次扩展矩阵，不重跑旧版，也不切换全局仓库。
- Grok 4.6三题作为标杆；Gemini 3.8 Flash三题观察弱模型改善；Codex不进入本轮评测。
- 新增任务只有Grok两篇与Gemini两篇。控制器支持`--parallel 4`，每篇独立目录、日志和状态文件，总清单仅汇总。
- 当前实验根目录为`/Users/anan/Desktop/paper-test/aiwritepaper-ab-2.1.0-rc.2`；动态页面展示7个精简样本。

## 候选验收

- 全量Python测试、Node SVG测试、19方向编译同步和Skill入口校验必须通过。
- `git diff --check`必须通过。
- 静态测试只能证明工程一致性，不证明论文质量。

## 正式版发布门

候选当前命名`2.1.0-rc.2`。以下精简真实测试未完成前，不升级稳定`2.1.0`：

1. 完成Grok 4.6与Gemini 3.8 Flash各三题样本，其中只新增各两题。
2. 专业已知错误归零：错接线/供电窗、APOS箭头、综述明确误分类。
3. 所有新增样本伪造事件为0；Gemini相对既有弱模型失败样本在完整性、配图和文档上能够观察到改善。
4. 强模型应生图任务的真实生图嵌入率为100%。
5. 评分由写作流程之外的统一评审者完成；不把本轮描述成严格版本因果A/B。

## 发布边界

评测控制器和文档可以随时提交GitHub候选分支，不再被18/30任务矩阵阻塞。完成上述4个新增样本及统一审阅后，再决定是否把版本改为2.1.0、更新稳定README并分发各Agent。
